{
  "name": "molly-portfolio-website",
  "author": "Tomer Aberbach <tomeraberbach@gmail.com> (https://tomeraberba.ch)",
  "scripts": {
    "start": "gulp",
    "clean": "gulp clean",
    "deploy": "gulp clean && git add . && git commit -m \"changes\" && git push && gulp && git add out && git commit -m \"out\" && git subtree split --prefix out -b master && git push -f origin master:master && git branch -D master && git reset --hard HEAD~1 && gulp clean"
  },
  "devDependencies": {
    "autoprefixer": "^9.5.1",
    "browser-sync": "^2.26.5",
    "cssnano": "^4.1.10",
    "del": "^4.1.1",
    "fs-extra": "^7.0.1",
    "gulp": "^4.0.1",
    "gulp-hb": "^8.0.0",
    "gulp-htmlmin": "^5.0.1",
    "gulp-imagemin": "^5.0.3",
    "gulp-markdownit": "^1.0.3",
    "gulp-postcss": "^8.0.0",
    "gulp-sass": "^4.0.2",
    "handlebars-helpers": "^0.10.0",
    "markdown-it-link-attributes": "^2.1.0",
    "standard": "*",
    "through2": "latest",
    "yaml": "^1.5.0"
  }
}
